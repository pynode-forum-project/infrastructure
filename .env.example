# Docker Compose Project Configuration
# Copy this file to .env and customize for your project

# Project name prefix (used for container names, networks, and volumes)
# This allows multiple instances of the same project to run simultaneously
COMPOSE_PROJECT_NAME=forum

# Port Configuration
# Change these if you have port conflicts with other projects

# Database Ports
MYSQL_PORT=3306
MONGODB_PORT=27017

# Message Queue Ports
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672

# Service Ports
GATEWAY_PORT=8080
AUTH_SERVICE_PORT=5000
USER_SERVICE_PORT=5001
POST_SERVICE_PORT=5002
HISTORY_SERVICE_PORT=5003
MESSAGE_SERVICE_PORT=5004
FILE_SERVICE_PORT=5005

# Frontend Port
FRONTEND_PORT=3000

# Email Configuration - Real SMTP Settings (REQUIRED)
# The email service now uses real SMTP by default. Configure your SMTP provider below.

# Gmail SMTP (default)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_DEFAULT_SENDER=noreply@forum.com

# Alternative SMTP Providers (uncomment and configure as needed):

# Outlook/Hotmail
# MAIL_SERVER=smtp-mail.outlook.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@outlook.com
# MAIL_PASSWORD=your-password
# MAIL_USE_TLS=true

# SendGrid
# MAIL_SERVER=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key
# MAIL_USE_TLS=true

# Mailgun
# MAIL_SERVER=smtp.mailgun.org
# MAIL_PORT=587
# MAIL_USERNAME=your-mailgun-username
# MAIL_PASSWORD=your-mailgun-password
# MAIL_USE_TLS=true

# Custom SMTP Server
# MAIL_SERVER=smtp.yourdomain.com
# MAIL_PORT=587
# MAIL_USERNAME=your-username
# MAIL_PASSWORD=your-password
# MAIL_USE_TLS=true

# Optional: MailHog Ports (only if using MailHog for local testing)
# Uncomment mailhog service in docker-compose.yaml if needed
# MAILHOG_SMTP_PORT=1025
# MAILHOG_WEB_PORT=8025

# Optional: For local testing with MailHog (uncomment to use)
# MAIL_SERVER=localhost
# MAIL_PORT=1025
# MAIL_USE_TLS=false
# MAIL_USERNAME=
# MAIL_PASSWORD=

# AWS S3 Configuration (for file uploads)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=forum-uploads
